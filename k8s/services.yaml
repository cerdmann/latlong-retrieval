apiVersion: v1
kind: Service
metadata:
  name: {{.Values.name}}-svc
spec:
  type: {{.Values.serviceType}}
  selector:
    app: {{.Values.name}}
  ports:
    - protocol: TCP
      port: {{.Values.servicePort}}
      targetPort: {{.Values.port}}
